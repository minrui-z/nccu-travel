# 驗證紀錄

以下記錄交付版本於 2026-09-09（本機時間）完成的發布前檢查。

| 項目 | 結果 |
|---|---|
| 計算、日期、匯率、草稿與欄位對應 | 200 項測試通過。 |
| 產品操作流程 | 隔離 Chromium 19 項全部通過；失敗、頁面例外與外站請求均為空。 |
| 原格式 XLS | 24 個區段變體、28 個輸出案例，5,768 項斷言通過。 |
| 獨立讀取 | xlrd 2.0.2 讀回 28 檔、2,455 個指定儲存格與富文字內容。 |
| 列印版面 | 相同渲染器與字型設定、單一程序及全新暫存設定，24 個日期變體通過；另驗證學生 7 欄私人日期補充版，仍為 A4 單頁。字型替代限制見下文。 |
| 一般版 | 各變體主表 1 張 A4 直式，原附表保留為第 2 張。 |
| 學生版 | 各變體均為 1 張 A4 直式。 |
| 正式建置 | TypeScript 與 Vite 建置通過；相對資產路徑及 25 個 XLS 核對通過。 |
| 正式 Worker | 在沒有 Node 全域的環境執行實際產物，兩版範例及全部 24 種欄數皆可匯出原生 XLS，共 32 個情境（含合併編輯、長文字及私人日期）。 |
| 程式檢查 | 應用程式 lint 通過；TypeScript 嚴格檢查通過。 |
| 套件檢查 | 本次安裝後 `npm audit` 未發現已知漏洞。 |

## 實際瀏覽器檢查

以 `/nccu-travel/` 專案子路徑開啟正式靜態產物，確認頁面、字型、資料、模板與 Worker 載入。本次 19 項產品流程結果見 [qa/product-flow-results.json](../qa/product-flow-results.json)，逐項範圍見 [UI-AUDIT.md](UI-AUDIT.md)。涵蓋空白表與四步驟往返、日期一次套用及縮短確認、合併與復原、私人行程、核定上限、單筆費用及獨立匯率、精確錯誤定位、原生 XLS 下載、唯讀範例不讀寫 IndexedDB、草稿備份／恢復與失敗保留、兩種桌面尺寸、長表單、放大預覽與 200% 等效重排。

先前整合檢查另涵蓋目的地日支額查詢、缺漏匯率提示、官方 CSV 日期與賣出欄、文字超量阻擋，以及一般／學生原生 XLS 的獨立讀回與列印。當時讀取姓名、總計、工作表與樣式數，再以同一可用字型設定渲染：一般版主表加附表共 2 頁、學生版 1 頁。

匿名算例總計為 **77,759 元**，申請上限為 **70,000 元**；日期合併與拆分前後相同。此算例僅測計算，內含舊範例數額，不能當成該日實際匯率、城市日支額或適用保費。

匯率 CSV 解析器另測官方買賣／現金／即期欄位、中文表頭、日期不符、HTML 驗證頁、缺漏資料及失敗保留原快照。更新腳本已用普通 HTTPS 請求取得真實臺銀 CSV；尚未在使用者的 GitHub runner 上執行。

學生版 7 欄另以實際正式 Worker 產出，獨立讀回 13–19 日全部日期，確認「無免費餐」及返國日生活費，並渲染、視覺檢查為單張 A4。後續文字適配修訂補齊生活費結果；只調整可填欄位的換行與字級，不變更原表幾何。

以下各節為歷次修訂的驗證快照，保留當時的測試數量及行為；最新總數以上表為準。私人日期目前固定顯示「個人行程」且地點選填，早期清空所有文字的行為已由後續修訂取代。填寫範例目前為獨立唯讀頁，不再載入或覆蓋草稿。

## 2026-09-09 修訂驗證

- 日期欄由一般 6／學生 7 擴充至兩版各 12，原 C:N 底層欄寬不變。原一般 6／學生 7 模板仍與提供的空白檔逐位元組一致。
- 實際瀏覽器重現自訂勾選框滑鼠點擊不更新、鍵盤空白鍵可以更新的情況；改用原生 checkbox 後，滑鼠勾選、取消勾選、拆分與合併均通過。選取狀態及不相容欄位原因直接顯示於按鈕下方。
- 實際瀏覽器拆分 07-15～07-16，再合併兩欄，逐日日期完整且總額保持 77,759。07-14 與 07-15 選取會顯示工作記要與午餐不同，不接受混合條件。
- 另以 256 種選取组合及 11 類相容性差異檢查，不漏日、不重複、不修改每日或類別金額。
- 學生上限 70,000 的正式 Worker 輸出保留計算 77,759、申請 70,000、超額 7,759 備註。分攤、零上限、無上限及備註超量均有回歸測試。
- 兩版 12 欄的正式 Worker 輸出以獨立讀取器讀回 13–24 日全部日期，另檢視 PDF，無漏日與遮蔽金額；一般主表與附表共 2 頁，學生 1 頁。窄欄短記號在原備註中說明。
- 學生 4 欄上限範例已檢視列印，超額說明完整位於原 C26 備註欄內。
- 網頁不再出現指定宣傳句、進位狀態提示或下載按鈕額外徽章。學生上限欄與不向本案報支差額已在瀏覽器檢查；下載操作回報 XLS 已產生。

上述列印檢查使用同一渲染器與明確設定的字型目錄。字型、樣式定義、列高欄寬、列印範圍及圖框以原 XLS 記錄比對；日期區的合併和框線樣式指派是使用者要求的變動，不能宣稱日期區與原 6／7 欄視覺完全相同。

## 可重跑的檢查

```sh
npm ci
npm test
npm run build
npm run test:build
npm run preview:pages
```

獨立讀取與列印使用 [qa/README.md](../qa/README.md) 中的操作。原始結果摘要存於 [qa/verification-summary.json](../qa/verification-summary.json)；產生的測試 XLS、PDF 和本機草稿不隨網站公開。

## 保留的驗收限制

- 計算順序未因文案修改而改變；來源與適用限制保留於 DATA.md。介面及檔名依使用者要求移除額外狀態字樣。
- 檢查保留了原字型記錄；本機替代字型的渲染不能證明不同 Excel 版本／字型環境的像素完全相同。原字型環境下的 Excel 列印驗收尚未完成。
- 文字容量檢查會阻擋已知超量輸入；未知字型替代、極端字串仍需實際列印核對。
- GitHub Pages 工作流程已準備，尚未在實際儲存庫啟用。發布後應依 DATA.md 完成 runner 的首次資料更新驗收；下載受限時保留舊資料與 CSV／人工輸入途徑。

## 國家城市與起迄欄位修訂

- 實際瀏覽器選臺灣後只列臺灣起迄城市；選美國後只列美國城市，切換日本後清空波士頓並只列日本城市。
- 交通工具歇夜可分別選起點與迄點，原表即時呈現臺北→美國波士頓。選地點不改原日支額；另選日支額地區後也不覆蓋路線。
- 重新整理後起點、迄點與日支額三組選單均依本機草稿恢復；切換普通公差日顯示留宿國家與城市。測試使用獨立本機測試網址與匿名資料，未取代使用者原草稿。
- 87項測試、型別與lint通過；正式Worker共28個匯出情境。兩版額外起迄範例均獨立讀回C13「臺北→美國波士頓」及C18「395×30%=118.5」。
- 新欄位仍映射到原表既有地點格，未修改原XLS樣式或列印設定。

## 合併編輯與自動排字修訂

- 103 項單元測試、應用程式 lint、正式建置通過。正式 Worker 共 30 個原生 XLS 匯出情境，包含兩版合併欄長文字。
- 原 XLS 保留路徑仍通過 5,768 項記錄比對；獨立 xlrd 再讀回 28 檔、2,455 個儲存格。24 組空白模板沒有改動。
- 新增 26 個自動排字 XLS 案例，以獨立 xlrd 驗證 171 個儲存格的文字、字級、換行與富文字。兩版完整長文字另以原字型環境列印並檢視，事由、工作記要、備註字元完整；一般版主表加原附表共 2 頁，學生版 1 頁。
- 使用者允許調整填入文字的字級與換行。僅新增 FONT／XF 副本並改可填格的指派，原字型名稱、原記錄、列高欄寬、框線、簽章及列印範圍保留；不代表字級調整後仍與原空白表逐像素相同。
- 桌面瀏覽器實測合併 07/15–07/16 同步修改工作記要及早餐，維持 4 欄；明確拆開後確認第二天完整保有文字與早餐，再合併恢復 4 欄。重新載入後資料與合併保留。
- 同一段兩行長工作記要，預覽自動使用 8.5pt，容量提示消失，兩版瀏覽器均回報「XLS 已產生」。最低 8pt 仍不足時保留全文並指出需精簡的欄位。
- 週末旗標與舊核准期間不再影響每日生活費、費用日期、雜費日數與分組；私人行程仍排除。匯率報價日、結匯有效期間及特定經費／艙等規則保留。
- 已執行 `npx impeccable install` 並讀取其設計指引；調整日期欄編輯範圍、整欄導覽、原生勾選、匯率展開區與膳宿說明。工具在開發階段使用，不加入網站執行依賴。

## 私人日期與選項切換完整性審查

- 全部 143 項單元測試、5,768 項原生 XLS 斷言、32 個正式 Worker 情境、lint 與建置通過。24 組一般變體保持原檔；發布另含學生 7 欄私人工作記要補充版，共 25 份空白模板。
- 私人日期切換清空地點、日支額、工作記要、扣款與膳宿，合併欄保持合併。瀏覽器實測 07/15–07/16 私人欄只有日期；切回出差欄位仍空白，重新載入後私人狀態和 4 欄配置保留。
- 原生 XLS 另以 xlrd 檢查一般 4 欄及學生 7 欄：私人日期下方 12 格全部空白。學生補充版的獨立讀取及 3 份 A4 單頁列印證據見 `qa/student-private-seven-report.json`；此改動只分開原共享工作記要，不宣稱像素相同。
- 資料新增至 414 報價日 × 19 幣別（2025-01-02–2026-09-08）。逐份核對快照、CSV 與 SHA-256。瀏覽器實查 2026-07-10：美元現金賣出 32.42000、歐元現金賣出 37.30000，使用當日靜態快照。
- 幣別由美元改歐元後舊匯率、日期、來源說明清空；重新查詢得到歐元報價。再變更日期會清空舊報價。信用卡填入 500 與手續費 15 後來回切換付款方式，舊結算額不會自動恢復。
- 新增 18 項回歸測試，涵蓋私人與交通狀態、航程供餐與會議供餐隔離、季節查價的資料缺漏、匯率與付款條件、保費確認、經費與艙等適用性。修改實際日期或私人日期範圍須重新確認保費；改變銀行匯率基準不會沿用舊銀行報價；有效結匯證明依自身日期重新驗證。
- 刪除最後機票後，隱藏艙等條件與附件不再阻擋；非國科會經費不檢查隱藏國科會活動身分。每筆外幣費用須有自身匯率日期，不偷用生活費日期。

- 最後實測學生 7 欄含 07/16、07/18、07/19 私人日期，私人欄下方空白，相鄰工作記要保留；日期修改後保費確認取消，重新核對後瀏覽器回報「XLS 已產生」。
- 修正瀏覽器替代字型寬度與估算不同造成的窄欄遮字：僅對預覽原有 shrink-to-fit 儲存格追加實際字寬量測，保留全文及原格大小。學生 C18「268×30%=80.4」文字寬 50.744 px，完整位於 54.597 px 的儲存格內；此頁三個縮字格均無水平越界。原生 XLS、金額與模板沒有因此改動。

## 費用匯率與單據操作修正（2026-09-09）

- 本輪共 157 項單元測試通過；原生 XLS 5,768 項斷言、28 份輸出，以及正式 Worker 32 情境維持通過。
- 以隔離 Chromium 在 `/nccu-travel/` 執行 12 項操作驗證：空白單據編號／張數下載、費用自動匯率、手動值保留、跨頁更新、錯誤日期與欄位聚焦、學生預覽頁碼、放大預覽鍵盤操作、720px 重排、容量阻擋、Worker 啟動失敗及草稿備份失敗恢復。無未捕捉錯誤，無外站資料請求。
- 測試使用匿名資料和獨立瀏覽器儲存，不覆蓋使用者草稿。完整結果見 `qa/ui-audit-results.json`；介面審查見 `docs/UI-AUDIT.md`。720px 是 1440px 桌面 200% 縮放的等效 CSS 寬度，並非讀屏器或所有瀏覽器認證。
- 實際瀏覽器下載的空白單據編號／張數 XLS，以獨立 xlrd 2.0.2 重新開啟，確認一般版兩張工作表、209 筆格式記錄與「附單據__張」保留。

## 各筆匯率、生活費結果與私人地點修正（2026-09-09）

- 173 項單元測試、5,768 項原生 XLS 斷言與 32 個正式 Worker 情境通過；lint 與正式建置通過。
- 每筆費用按自身幣別、匯率列示，不再由總計的生活費匯率代表全部費用。同格多筆保留個別換算，信用卡與臺幣不套外幣率。窄格用費用索引對照原表備註，完整金額、精度與匯率保留；索引不依賴單據號數。
- 生活費不再省略等號結果。優先完整算式；一般版極窄欄列完整金額，算式移至同表備註。數字1字寬、正常字型試算與費用結果的換行已修正；原欄寬、列高、字型名稱及列印範圍不變。
- 私人行程顯示「個人行程」，地點選填；其餘請領格維持原生空白。兩版瀏覽器實測合併三天同步修改、重新載入、清空地點及下載均通過，未動用使用者草稿。4 份實際瀏覽器下載 XLS，以獨立 xlrd 2.0.2 比對 462 格全部一致。
- 匿名一般／學生 7 欄另以獨立 xlrd 比對 231 格，並實際渲染列印。學生主表 1 張 A4；一般主表 1 張 A4，加原附表 1 張。已修正並重新檢視一般版陸運費與備註的裁行；完整生活費結果與各筆匯率均可讀。
- 本輪列印環境有原新細明體與 Times New Roman，但缺少原標楷體，明確使用 KaiTi 替代。這些檢查確認本環境文字及 A4 容納，不宣稱所有原字型環境逐像素一致。證據：`qa/report-details-ui.json`、`qa/report-details-readback.json`、`qa/report-details-print.json`。
